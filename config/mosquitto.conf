# Plain text MQTT protocol listener configuration
listener 1883 0.0.0.0

# Essential for local, zero-internet field operations inside deep-pit mines
allow_anonymous true
persistence false

# Keep logs light to avoid consuming local disk space
log_type error
log_type notice
connection_messages true